# assets/

Drop `logo.webp` here — the circular brand mark shown top-left, rendered inside
a white circular button at 72% (object-fit: contain). Referenced by index.html
as `assets/logo.webp`. Until it is added, the header shows an empty white circle.
