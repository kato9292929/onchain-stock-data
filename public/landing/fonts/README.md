# fonts/

Drop `GeistPixel-Circle.woff2` here — the FALLBACK display font (weight 400).
The primary display face is BubbledotICG-FinePos, loaded from OnlineWebFonts in
index.html, so this is only used if that CDN face fails. Until added, the
fallback chain resolves to monospace.
